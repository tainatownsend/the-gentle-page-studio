# Steady Current acceptance expectations

The corrected build is acceptable only when the source manuscript is treated as authoring syntax and the publication is treated as the customer artifact.

Reader-facing output must never expose authoring markup. Structured tools must remain structured. The same normalized publication model must drive Preview, static output, and fillable output.
