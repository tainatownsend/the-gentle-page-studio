# Inline

`Inline` arranges children horizontally without wrapping.

```tsx
<Inline gap="sm" align="center" justify="between">
  <Text>Autosave</Text>
  <Switch label="Enable autosave" />
</Inline>
```

Wrapping belongs to the future `Cluster` primitive rather than `Inline`.
