# Stack

`Stack` arranges children vertically.

```tsx
<Stack gap="md">
  <Text as="h2" variant="h2">Publication details</Text>
  <Field label="Title"><Input /></Field>
</Stack>
```

`gap` uses the existing semantic spacing tokens and defaults to `md`.
