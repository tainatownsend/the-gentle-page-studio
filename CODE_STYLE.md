# Code Style

- Prefer readable names over comments.
- Document why a decision exists, not what a line of code does.
- Use absolute imports through `@/`.
- Keep domain logic inside `studio/`.
- Keep design-system components domain-agnostic.
- Avoid premature abstractions.
- Use strict TypeScript and do not silence errors without a documented reason.
